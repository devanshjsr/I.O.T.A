import 'package:flutter/material.dart';

class SubjectSettings extends StatelessWidget {
  static const routeName = "/subject_settings";
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Subject Settings"),
    );
  }
}
